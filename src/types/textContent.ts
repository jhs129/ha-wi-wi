export type TextContent = {
  unauthorized: {
    title: string
    subtitle: string
    passwordPlaceholder: string
    submitButton: string
  }
  authorized: {
    title: string
    description: string
  }
} 